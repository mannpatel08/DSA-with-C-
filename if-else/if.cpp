#include<iostream>
using namespace std;

int main()
{
    int num = 0;

    if (num > 0)
    {
        cout<<"Number is Positive";
    }

     if (num < 0)
    {
        cout<<"Number is Negative";
    }

     if (num == 0)
    {
        cout<<"Number is Netrual";
    }
    
}